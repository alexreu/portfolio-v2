export const SectionSubtitle = ({ children }) => {
    return (
        <span className="section-subtitle inline-block mb-5 lg:mb-3 text-base tracking-wide leading-4 font-main font-semibold">
            {children}
        </span>
    );
};
